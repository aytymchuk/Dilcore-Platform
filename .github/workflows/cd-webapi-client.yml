name: 🚀 CD WebAPI Client

on:
  push:
    branches: ["main"]
    paths:
      - 'src/WebApi.Client/**'
      - '.github/workflows/cd-webapi-client.yml'
  workflow_dispatch:

jobs:
  publish:
    uses: ./.github/workflows/nuget-publish-template.yml
    with:
      project_path: 'src/WebApi.Client/WebApi.Client.csproj'
      tag_name: 'webapi-client'
      bump_type: 'patch'
    secrets: inherit