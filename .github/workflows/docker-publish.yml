name: 🐳 Build and Push Docker Image

"on":
  push:
    branches: ["main"]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required for pushing tags
      packages: write  # Required for pushing to GHCR

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0  # Required to fetch all tags for git describe

      - name: 🏷️ Calculate Version
        id: calculate_version
        run: |
          set -euo pipefail
          # Fetch all tags is handled by fetch-depth: 0 in checkout step
          
          # Get the latest tag, default to v0.0.0 if no tags exist
          # We use 2>/dev/null to suppress error if no tags found
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          VERSION=${LATEST_TAG#v}
          
          echo "Latest tag found: $LATEST_TAG"
          
          if ! [[ "$LATEST_TAG" =~ ^v([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            echo "::error::Latest tag '$LATEST_TAG' is not strict semver (expected vMAJOR.MINOR.PATCH)."
            exit 1
          fi
          
          MAJOR="${BASH_REMATCH[1]}"
          MINOR="${BASH_REMATCH[2]}"
          PATCH="${BASH_REMATCH[3]}"
          
          # Increment patch version
          NEW_PATCH=$((PATCH + 1))
          
          # Construct new version
          NEW_VERSION="$MAJOR.$MINOR.$NEW_PATCH"
          
          echo "Previous version: $VERSION"
          echo "New version: $NEW_VERSION"
          
          # Output variables for next steps
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "tag=v$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: 🖊️ Create and Push Tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          TAG_NAME="${{ steps.calculate_version.outputs.tag }}"
          
          # Check if tag exists remotely
          if git ls-remote --exit-code --tags origin "$TAG_NAME" >/dev/null 2>&1; then
            echo "::error::Tag $TAG_NAME already exists on remote. Aborting."
            exit 1
          fi

          # Check if tag exists locally
          if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
             echo "::error::Tag $TAG_NAME already exists locally. Aborting."
             exit 1
          fi

          echo "Creating tag $TAG_NAME"
          git tag -a "$TAG_NAME" -m "Release $TAG_NAME"
          
          echo "Pushing tag $TAG_NAME"
          git push origin "$TAG_NAME" || {
            echo "::error::Failed to push tag $TAG_NAME. It might have been created by another workflow."
            exit 1
          }

      - name: 🔠 Lowercase Image Name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: 🔐 Log in to the Container registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef # v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: ✅ Verify Dockerfile exists
        run: |
          if [ ! -f "src/WebApi/Dockerfile" ]; then
            echo "::error::Dockerfile not found at src/WebApi/Dockerfile"
            exit 1
          fi

      - name: 🧰 Set up Docker Buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 # v3

      - name: 🐳 Build and push Docker image
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 # v5
        with:
          context: .
          file: src/WebApi/Dockerfile
          push: true
          provenance: mode=max
          sbom: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.calculate_version.outputs.version }}
          build-args: |
            BUILD_VERSION=${{ steps.calculate_version.outputs.version }}
